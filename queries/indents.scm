[
  (block)
  (parameters)
  (arguments)
  (import_list)
  (generic_type)
] @indent.begin

[
  ")"
  "]"
  "}"
] @indent.end

; Indent the body of match statement specifically if needed, 
; but (block) should cover it.
